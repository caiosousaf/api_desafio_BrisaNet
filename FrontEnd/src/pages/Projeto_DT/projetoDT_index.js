import Sidebar from '../../components/SideBar/SideBar'

const projetoDT_index = () => {
    return(
        <div>
            <Sidebar />
            Projeto especifico
        </div>
    );
}

export default projetoDT_index;